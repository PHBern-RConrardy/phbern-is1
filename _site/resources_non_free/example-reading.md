# Protected example resource

This placeholder represents a licensed or otherwise restricted course
resource.

The directory name triggers the lock marker in `phbern-typst`. Real access
control must be configured on the publishing platform.
